import Typography from "@mui/material/Typography"
export default function NotFound() {
    return (
        <>
            <Typography variant="h4" > Could not find the requested timetable</Typography>
        </>
    )
}